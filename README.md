# Tavola Chiringo

Proyecto estatico listo para Cloudflare Pages.

Configuracion recomendada:

- Build command: dejar vacio
- Build output directory: `public`

Para despliegue directo con Wrangler, `wrangler.toml` ya apunta a `./public` mediante `[assets]`.

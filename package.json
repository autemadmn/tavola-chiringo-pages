{
  "name": "tavolaautemtry",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "snapshot:menu": "node scripts/refresh-menu-snapshot.mjs",
    "build": "npm run snapshot:menu && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.8"
  },
  "devDependencies": {
    "vite": "^8.1.5"
  }
}
